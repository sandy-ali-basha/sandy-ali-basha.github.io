# Sandy Portfolio (React)

This project now runs as a standard React app using Vite.

## Run locally

```bash
npm install
npm run dev
```

Then open the URL shown in the terminal (usually `http://localhost:5173`).

## Other scripts

```bash
npm run build
npm run preview
```

> Use `npm run dev` (not `npm start`).
