# Sandy Portfolio (React)

This portfolio was converted from static HTML to a React-based app.

## Run locally

Use any static server (for example):

```bash
python -m http.server 4173
```

Then open `http://localhost:4173`.
